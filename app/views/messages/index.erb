<% @messages.each do |message| %>
  <div class="message">
    <h3><a href="/messages/<%= message.id %>"><%= message.content %></a> <small>(by: <%= message.author %>)</small> <a href="<%= message.url %>"><small>(url: <%= message.url %>)</small></a></h3>
  </div>
<% end %>
  <div>
  <a href="/messages/new">Post a message... It's FREE (for now)!</a>
  </div>
